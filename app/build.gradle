apply plugin: 'com.android.application'

android {
    compileSdkVersion 27

    defaultConfig {
        applicationId 'org.bubenheimer.fgsvcbpbug'
        minSdkVersion 26
        targetSdkVersion 27
        versionName '1.0'
        versionCode 1
        resConfigs 'en', 'en-rUS'
    }
}
